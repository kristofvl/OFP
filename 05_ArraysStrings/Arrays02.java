/**  Fuelle erst ein Array 'liste' von Laenge 30 mit beliebigen Symbolen (char)
     zwischen 65 und 90.
     Gebe dann liste in der Konsole gemischt aus, so dass zuerst das erste Element
     gedruckt wird, dann das mittlere Element, dann das zweite Element, dann das
     Element nach dem mittleren Element und so weiter.
  */
import java.util.Random;   // random number generator

class Arrays01 {
  public static void main( String[] str ) {
    Random rand = new Random();
    // Tipp: rand.nextInt( 10 ); liefert eine beliebige Zahl zwischen 0 und 10

  }
}
